# server port config
server:
  port: 8001
  
# oracle driver config
spring:
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@localhost:1521:xe
    username : C##GOODEAT
    password : GOODEAT
    hikari:
      auto-commit: false

# jpa config
  jpa:
    generate-ddl: false
    show-sql: true
    database: oracle
    properties:
      hibernate:
        '[format_sql]': true
        
  thymeleaf:
    cache: false # 기본 값 true / 개발 중 변경 사항 즉시 확인하기 위해 캐시 false
    
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
      # 업로드 된 파일의 임시 저장 공간
      
      location: C:\Lecture\ShareFolder\project\goodeat
  mail:
    host: smtp.gmail.com
    port: 8001
    username: inhere31@gmail.com
    password: goodeat1234!
    properties:
      mail:
        smtp:
          starttls:
            enable: true
            required: true
          auth: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
    

image:
  image-dir: src/main/resources
     
logging:
  level:
    root: info
